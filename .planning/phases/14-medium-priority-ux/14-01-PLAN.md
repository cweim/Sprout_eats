---
phase: 14-medium-priority-ux
plan: 01
type: execute
---

<objective>
Map view improvements: auto-center on user location and show filter counts.

Purpose: Users lack spatial context on load; filter chips don't show distribution.
Output: Map auto-centers on user if location granted; filter chips show counts.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/UX_AUDIT.md
@webapp/app.js
@webapp/index.html
@webapp/styles.css

**From UX Audit:**
> 🟡 **MEDIUM: Map loads without user location context**
> - Map shows all places, may be zoomed out too far
> - User doesn't know where they are relative to places
> - **Fix:** Auto-center on user location if granted, show blue dot

> 🟡 **MEDIUM: Filter chips don't show counts**
> - "All" / "To Visit" / "Visited ✓" have no numbers
> - **Fix:** Add counts: "All (15)" / "To Visit (12)" / "Visited ✓ (3)"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-center map on user location</name>
  <files>webapp/app.js</files>
  <action>
Modify map initialization to request user location early and center on it:

1. In initializeApp() or loadPlaces():
   - Request geolocation with navigator.geolocation.getCurrentPosition()
   - If granted and within 200km of any place, center map on user
   - If denied/error or no places nearby, fitBounds on all places (current behavior)

2. Update displayPlacesOnMap():
   - If userLocation exists and fitAll=true, include user in bounds consideration
   - Otherwise center on user with zoom 13

3. Ensure blue dot marker shows user position (already exists as userLocationMarker)

Important: Don't block on geolocation - request async, apply if available before fitBounds.
  </action>
  <verify>Open app with location granted → map centers on you instead of places</verify>
  <done>Map auto-centers on user location when granted, with places visible</done>
</task>

<task type="auto">
  <name>Task 2: Add filter chip counts</name>
  <files>webapp/app.js, webapp/index.html</files>
  <action>
Add counts to all filter chips:

1. Map filter chips (index.html + app.js):
   - "All (15)" / "To Visit (12)" / "Visited ✓ (3)"
   - Update counts when places loaded or filtered
   - Create updateFilterCounts() function called after loadPlaces()

2. List view visited filter chips:
   - Same pattern: "All (X)" / "To Visit (Y)" / "Visited (Z)"
   - Update in applyFilters() after counting filtered results

3. Reviews tab filter chips:
   - "All (X)" / "With Photos (Y)" / "5 ⭐ (Z)" / "4 ⭐ (W)"
   - Update in renderReviewsList() or applyReviewFilters()

Use span.chip-count inside each chip button (already exists in HTML, just populate).
  </action>
  <verify>Load app with places → all filter chips show counts in parentheses</verify>
  <done>All filter chips (map, list, reviews) show item counts</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Map centers on user location if permission granted
- [ ] Map falls back to fitBounds if location denied/unavailable
- [ ] All filter chips show counts (map, list, reviews tabs)
- [ ] Counts update when filters change
- [ ] No console errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Spatial context improved for users
</success_criteria>

<output>
After completion, create `.planning/phases/14-medium-priority-ux/14-01-SUMMARY.md`
</output>
