---
phase: 13-quick-wins
plan: 02
type: execute
---

<objective>
Add button feedback states and improve error handling UX.

Purpose: Buttons lack tactile feedback. Errors have no retry option.
Output: :active states on buttons, retry buttons on errors, disabled states with tooltips.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/UX_AUDIT.md
@webapp/styles.css
@webapp/app.js
@webapp/index.html

**From UX Audit:**
> 🟡 **MEDIUM: No :active states on buttons**
> - Buttons don't feel tappable
> - **Fix:** Add :active state with scale/shadow

> 🟡 **MEDIUM: Network errors not retryable**
> - No "Retry" button when things fail
> - **Fix:** Add "Retry" button on network errors

> 🟡 **MEDIUM: Review button enabled when not visited**
> - Confusing - can tap but then told to visit first
> - **Fix:** Disable button and show tooltip: "Mark as visited first"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add :active states to all buttons</name>
  <files>webapp/styles.css</files>
  <action>
Add :active pseudo-class to all button types for tactile feedback:

```css
/* Global button active state */
button:active,
.btn:active,
.toggle-btn:active,
.card-action-btn:active,
.popup-action-btn:active,
.filter-chip:active,
.visited-chip:active,
.map-filter-chip:active,
.review-filter-chip:active,
.sheet-btn:active,
.modal-btn:active,
.drawer-btn:active {
    transform: scale(0.95);
    opacity: 0.9;
}
```

Also add to:
- .sprout-toggle:active
- .more-btn:active
- .photo-add-btn:active
- .fab-discover:active

Ensure transition is smooth (add transition if not present).
  </action>
  <verify>Check styles.css for :active rules on button classes</verify>
  <done>All buttons have tactile :active feedback</done>
</task>

<task type="auto">
  <name>Task 2: Add retry button to error states</name>
  <files>webapp/app.js, webapp/styles.css</files>
  <action>
Add retry functionality to network error scenarios:

1. In loadPlaces() error handling:
   - Show error state with retry button
   - Retry button calls loadPlaces() again

2. In saveReview() error:
   - Current: just shows toast
   - Add: option to retry (could keep data in form)

3. In API calls with error states:
   - Add showErrorWithRetry(message, retryFn) helper
   - Shows toast/modal with "Retry" button

Example implementation:
```javascript
function showErrorWithRetry(message, retryFn) {
    // Show error toast with retry action
    const toast = showToast(message);
    // Or use a modal with retry button
}
```

Focus on critical paths: load places, save review, delete place.
  </action>
  <verify>Test error scenarios show retry option</verify>
  <done>Network errors have retry buttons</done>
</task>

<task type="auto">
  <name>Task 3: Disable review button when not visited</name>
  <files>webapp/app.js, webapp/styles.css</files>
  <action>
Prevent confusion by disabling review button for unvisited places:

1. In createPopupContent():
   - If !place.is_visited: add 'disabled' class to review button
   - Add title="Mark as visited first" tooltip

2. In createPlaceCard():
   - If !place.is_visited: add 'disabled' class to review button
   - Add title tooltip

3. In CSS:
   - .review-btn.disabled { opacity: 0.5; pointer-events: none; cursor: not-allowed; }
   - Or use actual disabled attribute

4. Alternative: Keep button enabled but show helpful message when clicked:
   - "Mark as visited first to write a review!"

Choose approach that feels most intuitive.
  </action>
  <verify>Click review on unvisited place - should be disabled or show message</verify>
  <done>Review button indicates it needs visited status</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All buttons have :active scale feedback
- [ ] Error states have retry option
- [ ] Review button disabled/grayed for unvisited places
- [ ] No console errors
- [ ] Transitions smooth (not jarring)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Buttons feel more responsive and interactive
- Error recovery is possible
</success_criteria>

<output>
After completion, create `.planning/phases/13-quick-wins/13-02-SUMMARY.md`
</output>
