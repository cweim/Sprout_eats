---
phase: 03-multi-place-support
plan: 01
type: execute
---

<objective>
Update backend services to support multiple place results and individual place deletion.

Purpose: Enable the foundation for users to see and select from multiple detected places.
Output: Places service returns up to 5 results, repository supports delete_place().
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior plans:**
@.planning/phases/02-reliability/02-02-SUMMARY.md (friendly errors, timeout)
@.planning/phases/01-testing-foundation/01-04-SUMMARY.md (repository tests)

**Source files to modify:**
@services/places.py
@database/repository.py
@tests/test_places.py
@tests/test_repository.py

**Tech stack available:** pytest, pytest-asyncio, aioresponses, tenacity
**Established patterns:** aioresponses HTTP mocking, class-based test organization, test_db fixture
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update places service to return multiple results</name>
  <files>services/places.py, tests/test_places.py</files>
  <action>
1. Modify search_place() to accept optional max_results parameter (default 1 for backwards compat):
   - Change return type to list[PlaceResult] when max_results > 1
   - Parse all places from API response up to max_results
   - Return empty list instead of None when no results

2. Update search_places_from_text() to pass max_results=5 to search_place():
   - Return up to 5 PlaceResult objects
   - Keep existing behavior of searching with full text

3. Add tests to tests/test_places.py:
   - TestSearchPlace: test_search_place_multiple_results - mock API returning 3 places, verify all returned
   - TestSearchPlacesFromText: test_returns_up_to_five_places - mock API with 5+ results, verify max 5 returned

Use aioresponses mocking pattern from existing tests.
  </action>
  <verify>pytest tests/test_places.py -v shows all tests passing including new multi-result tests</verify>
  <done>search_place() can return multiple results, search_places_from_text() returns up to 5</done>
</task>

<task type="auto">
  <name>Task 2: Add delete_place to repository</name>
  <files>database/repository.py, tests/test_repository.py</files>
  <action>
1. Add delete_place(place_id: int) -> bool to repository.py:
   - Query place by id
   - If found, delete and return True
   - If not found, return False

2. Add get_place_by_id(place_id: int) -> Optional[Place] to repository.py:
   - Query place by id
   - Return Place or None

3. Add tests to tests/test_repository.py:
   - TestDeletePlace class:
     - test_delete_existing_place: Add place, delete by id, verify not in get_all_places()
     - test_delete_nonexistent_place: Delete with invalid id, returns False
     - test_delete_returns_true: Verify delete returns True on success
   - TestGetPlaceById class:
     - test_get_existing_place: Add place, retrieve by id, verify fields match
     - test_get_nonexistent_place: Get with invalid id, returns None

Use test_db_with_repository fixture from conftest.py.
  </action>
  <verify>pytest tests/test_repository.py -v shows all tests passing including new delete tests</verify>
  <done>delete_place() and get_place_by_id() work, 5 new tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_places.py -v` passes all tests
- [ ] `pytest tests/test_repository.py -v` passes all tests
- [ ] `pytest tests/ -v` passes ALL project tests (no regressions)
- [ ] search_places_from_text() returns up to 5 results
- [ ] delete_place() correctly removes a place by id
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Backend ready for multi-place UI in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-place-support/03-01-SUMMARY.md`

Final summary should note:
- API changes to search_place() signature
- New repository functions added
- Test count increase
- Ready for 03-02 (handlers update)
</output>
