---
phase: 03-multi-place-support
plan: 02
type: execute
---

<objective>
Update bot handlers to show place selection and add delete command.

Purpose: Let users choose which places to save from multiple results and delete individual places.
Output: Inline keyboard for place selection, /delete command with place picker.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior plan:**
@.planning/phases/03-multi-place-support/03-01-SUMMARY.md (multi-result places, delete_place)

**Source files to modify:**
@bot/handlers.py
@run.py

**Design constraints from PROJECT.md:**
- Bot personality: Cute, friendly, encouraging tone
- Celebrate user actions (place saved, etc.)

**Tech stack available:** python-telegram-bot with InlineKeyboardButton/Markup (already used)
**Established patterns:** action_callback handler, get_menu_keyboard(), user_data for state
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update handle_url to show place selection</name>
  <files>bot/handlers.py</files>
  <action>
1. Import search_places_from_text (already exists but use multi-result now)

2. Modify handle_url() flow:
   - Instead of calling search_place() directly, use search_places_from_text()
   - If multiple places found (>1):
     - Store places in context.user_data["pending_places"] as list of dicts
     - Store source_url and platform in user_data
     - Show inline keyboard with place options:
       - Each button: place.name (truncated to 30 chars if needed)
       - Callback data: "select_place_{index}" where index is 0-based
     - Message: "I found {n} places! Which one did you mean? 📍"
   - If single place found:
     - Keep current auto-save behavior
   - If no places found:
     - Keep current manual input behavior

3. Use friendly tone in messages:
   - Multiple: "I found {n} places! Which one did you mean? 📍"
   - Single: Keep existing "Found and saved: {name}" with location pin

Keep backwards compatibility - single result still auto-saves.
  </action>
  <verify>Manual test: Send a URL, verify multiple places show as buttons (if API returns multiple)</verify>
  <done>Multiple places detected shows selection keyboard, single place auto-saves</done>
</task>

<task type="auto">
  <name>Task 2: Add place selection callback handler</name>
  <files>bot/handlers.py, run.py</files>
  <action>
1. Add select_place_callback() handler in handlers.py:
   - Pattern: callback_data starts with "select_place_"
   - Extract index from callback data
   - Get place from context.user_data["pending_places"][index]
   - Save to database using repository.add_place()
   - Clear pending_places from user_data
   - Delete selection message
   - Send location pin + confirmation: "Saved: {name}! 🎉\n{address}"

2. Register handler in run.py:
   - Add CallbackQueryHandler(select_place_callback, pattern="^select_place_")
   - Place after existing callback handlers

3. Handle edge case: If pending_places not in user_data:
   - Reply: "Hmm, that selection expired. Send the link again! 🔄"
  </action>
  <verify>Manual test: Select a place from the keyboard, verify it saves and shows confirmation</verify>
  <done>Place selection works, saves correctly, shows friendly confirmation</done>
</task>

<task type="auto">
  <name>Task 3: Add delete place command and callback</name>
  <files>bot/handlers.py, run.py</files>
  <action>
1. Add delete_command() handler in handlers.py:
   - Get all places from repository.get_all_places()
   - If no places: "No places to delete! Your list is empty. 📭"
   - If places exist:
     - Show inline keyboard with place names
     - Each button: "{place.name}" (truncated to 25 chars)
     - Callback data: "delete_place_{place.id}"
     - Message: "Which place would you like to remove? 🗑️"

2. Add delete_place_callback() handler:
   - Pattern: callback_data starts with "delete_place_"
   - Extract place_id from callback data
   - Call repository.delete_place(place_id)
   - If deleted: Update message to "Removed! Your places are tidied up. ✨"
   - If not found: "That place was already gone! 👻"

3. Register handlers in run.py:
   - CommandHandler("delete", delete_command)
   - CallbackQueryHandler(delete_place_callback, pattern="^delete_place_")

4. Update start_command() menu to include delete button:
   - Add "🗑️ Delete Place" button alongside existing "🗑 Clear All"
   - Or replace "🗑 Clear All" row with two buttons: "🗑️ Delete One" and "🗑️ Clear All"
  </action>
  <verify>Manual test: /delete shows places, selecting one removes it from list</verify>
  <done>/delete command works, place deletion confirmed with friendly message</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/ -v` passes ALL tests (no regressions)
- [ ] Multiple places from URL shows selection keyboard
- [ ] Selecting a place saves it and shows confirmation
- [ ] /delete command shows place list
- [ ] Deleting a place removes it and shows confirmation
- [ ] All messages use friendly, cute tone with emojis
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Multi-place selection works end-to-end
- Delete functionality works end-to-end
- Phase 3 complete: Multi-Place Support shipped
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-place-support/03-02-SUMMARY.md`

Final summary should note:
- New user flows (place selection, delete)
- Handler additions
- UI changes (new buttons)
- Phase 3 complete, ready for Phase 4 (Enhanced Metadata)
</output>
