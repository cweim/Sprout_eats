---
phase: 01-testing-foundation
plan: 02
type: execute
---

<objective>
Add unit tests for services/downloader.py - pure functions that don't require mocking.

Purpose: Test URL detection and file cleanup logic which are critical for the video processing pipeline.
Output: tests/test_downloader.py with comprehensive tests for detect_platform, is_valid_url, cleanup_files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Source file to test:**
@services/downloader.py

**Test infrastructure (from plan 01-01):**
@tests/conftest.py
@pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test detect_platform() and is_valid_url()</name>
  <files>tests/test_downloader.py</files>
  <action>
Create tests/test_downloader.py with TestDetectPlatform and TestIsValidUrl classes:

TestDetectPlatform:
- test_instagram_reel_url → returns "instagram"
- test_instagram_post_url → returns "instagram"
- test_instagram_short_url (instagr.am) → returns "instagram"
- test_tiktok_video_url → returns "tiktok"
- test_tiktok_short_url (vm.tiktok.com) → returns "tiktok"
- test_youtube_url → returns None
- test_random_url → returns None
- test_empty_string → returns None

TestIsValidUrl:
- test_valid_instagram → True
- test_valid_tiktok → True
- test_invalid_youtube → False
- test_invalid_random → False

Use realistic URLs like:
- https://www.instagram.com/reel/ABC123/
- https://www.tiktok.com/@user/video/1234567890
- https://vm.tiktok.com/ABC123/
  </action>
  <verify>pytest tests/test_downloader.py -v shows all tests passing</verify>
  <done>All detect_platform and is_valid_url test cases pass</done>
</task>

<task type="auto">
  <name>Task 2: Test cleanup_files()</name>
  <files>tests/test_downloader.py</files>
  <action>
Add TestCleanupFiles class to test_downloader.py:

- test_cleanup_existing_files: Create temp files, call cleanup_files(), verify they're deleted
- test_cleanup_nonexistent_files: Pass non-existent paths, verify no errors raised
- test_cleanup_mixed_files: Mix of existing and non-existing, existing ones deleted
- test_cleanup_none_path: Pass None as path, verify no errors

Use tmp_path pytest fixture for creating temporary test files.
Do NOT use unittest.mock for this - use real temp files.
  </action>
  <verify>pytest tests/test_downloader.py::TestCleanupFiles -v shows all tests passing</verify>
  <done>All cleanup_files test cases pass, temp files properly cleaned up</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_downloader.py -v` shows all tests passing
- [ ] Tests cover edge cases (empty string, None, invalid URLs)
- [ ] No warnings or deprecation notices
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- test_downloader.py has 12+ test cases
- Tests are readable and follow project conventions
</success_criteria>

<output>
After completion, create `.planning/phases/01-testing-foundation/01-02-SUMMARY.md`
</output>
