---
phase: 01-testing-foundation
plan: 04
type: execute
---

<objective>
Add unit tests for database/repository.py - CRUD operations with test database.

Purpose: Test database operations including deduplication logic using an in-memory test database.
Output: tests/test_repository.py with tests for all repository functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Source files to test:**
@database/repository.py
@database/models.py

**Test infrastructure:**
@tests/conftest.py
@pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test add_place() with deduplication logic</name>
  <files>tests/test_repository.py</files>
  <action>
Create tests/test_repository.py using the test_db fixture from conftest.py:

TestAddPlace:
- test_add_place_minimal: Add place with only required fields (name, lat, lng), verify returned Place
- test_add_place_full: Add place with all fields, verify all fields stored correctly
- test_add_place_duplicate_google_id: Add same google_place_id twice, verify returns existing (not duplicate)
- test_add_place_duplicate_without_google_id: Add two places without google_place_id, verify both created (no dedup)
- test_add_place_with_source_url: Verify source_url and source_platform stored correctly

Important: Each test should use the test_db fixture to ensure clean database state.
Verify by querying the database after add_place() to confirm data persisted.
  </action>
  <verify>pytest tests/test_repository.py::TestAddPlace -v shows all tests passing</verify>
  <done>add_place tests pass including deduplication edge cases</done>
</task>

<task type="auto">
  <name>Task 2: Test get_all_places(), get_place_count(), clear_all_places()</name>
  <files>tests/test_repository.py</files>
  <action>
Add test classes for remaining repository functions:

TestGetAllPlaces:
- test_get_all_places_empty: No places, returns empty list
- test_get_all_places_multiple: Add 3 places, verify all returned
- test_get_all_places_order: Verify ordered by created_at desc (newest first)

TestGetPlaceCount:
- test_get_place_count_empty: No places, returns 0
- test_get_place_count_multiple: Add 3 places, returns 3

TestClearAllPlaces:
- test_clear_all_places: Add 3 places, clear, verify count is 0
- test_clear_all_places_returns_count: Verify returns number of deleted places
- test_clear_all_places_empty: Clear when empty, returns 0 (no error)

Use test_db fixture for all tests. Add helper to create multiple places quickly.
  </action>
  <verify>pytest tests/test_repository.py -v shows all tests passing</verify>
  <done>All repository tests pass, 13+ test cases total</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_repository.py -v` shows all tests passing
- [ ] `pytest tests/ -v` shows ALL project tests passing (downloader, places, repository)
- [ ] Database isolation works (tests don't affect each other)
- [ ] Coverage for all repository functions
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- test_repository.py has 13+ test cases
- Full test suite passes: `pytest tests/ -v`
- Phase 1 complete: Testing foundation established
</success_criteria>

<output>
After completion, create `.planning/phases/01-testing-foundation/01-04-SUMMARY.md`

Final summary should note:
- Total test count across all test files
- Test command: `pytest tests/ -v`
- Coverage command: `pytest tests/ --cov=. --cov-report=term-missing`
- Phase 1 complete, ready for Phase 2 (Reliability)
</output>
