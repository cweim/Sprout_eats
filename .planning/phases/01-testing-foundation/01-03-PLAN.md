---
phase: 01-testing-foundation
plan: 03
type: execute
---

<objective>
Add unit tests for services/places.py - async functions requiring HTTP mocking.

Purpose: Test Google Places API integration with mocked responses to ensure correct parsing and error handling.
Output: tests/test_places.py with async tests for search_place and search_places_from_text.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Source file to test:**
@services/places.py

**Test infrastructure:**
@tests/conftest.py
@pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test search_place() with mocked HTTP responses</name>
  <files>tests/test_places.py</files>
  <action>
Create tests/test_places.py with async tests using aioresponses:

TestSearchPlace:
- test_search_place_success: Mock successful API response, verify PlaceResult fields
- test_search_place_no_results: Mock empty places array, verify returns None
- test_search_place_no_api_key: Monkeypatch GOOGLE_API_KEY to None, verify raises ValueError
- test_search_place_with_region: Verify regionCode is passed in request body

Mock the Google Places API response format:
```python
{
    "places": [{
        "displayName": {"text": "Test Cafe"},
        "formattedAddress": "123 Test St",
        "location": {"latitude": 1.234, "longitude": 5.678},
        "id": "place_id_123"
    }]
}
```

Use aioresponses context manager for mocking aiohttp.ClientSession.post.
Use monkeypatch fixture to temporarily set/unset config.GOOGLE_API_KEY.
  </action>
  <verify>pytest tests/test_places.py::TestSearchPlace -v shows all tests passing</verify>
  <done>search_place tests pass with proper mocking of HTTP and config</done>
</task>

<task type="auto">
  <name>Task 2: Test search_places_from_text()</name>
  <files>tests/test_places.py</files>
  <action>
Add TestSearchPlacesFromText class:

- test_search_places_from_text_success: Mock search_place to return a result, verify list returned
- test_search_places_from_text_empty_text: Pass empty string, verify returns empty list
- test_search_places_from_text_no_results: Mock search_place to return None, verify returns empty list

Note: search_places_from_text internally calls search_place, so mock at the HTTP level (aioresponses) rather than patching search_place directly. This tests the actual integration.
  </action>
  <verify>pytest tests/test_places.py -v shows all tests passing</verify>
  <done>All places.py tests pass, covering success and error paths</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_places.py -v` shows all tests passing
- [ ] Async tests run without warnings
- [ ] aioresponses properly mocks HTTP calls (no real API calls made)
- [ ] Config monkeypatching works correctly
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- test_places.py has 7+ test cases
- No real HTTP calls made during tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-testing-foundation/01-03-SUMMARY.md`
</output>
