---
phase: 01-testing-foundation
plan: 01
type: execute
---

<objective>
Set up pytest infrastructure for the discovery-bot project.

Purpose: Establish the testing foundation that all subsequent test plans will build upon.
Output: Working pytest setup with conftest.py, requirements-dev.txt, and passing empty test suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md

**Key files:**
@config.py
@database/models.py
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create requirements-dev.txt with test dependencies</name>
  <files>requirements-dev.txt</files>
  <action>
Create requirements-dev.txt with testing dependencies:
- pytest (test runner)
- pytest-asyncio (async test support - needed for aiohttp-based services)
- pytest-cov (coverage reporting)
- aioresponses (mock aiohttp calls for places.py tests)

Include -r requirements.txt to inherit production dependencies.
Do NOT add unnecessary dependencies like pytest-mock (unittest.mock is sufficient).
  </action>
  <verify>cat requirements-dev.txt shows all dependencies listed</verify>
  <done>requirements-dev.txt exists with pytest, pytest-asyncio, pytest-cov, aioresponses</done>
</task>

<task type="auto">
  <name>Task 2: Create tests directory with conftest.py and pytest configuration</name>
  <files>tests/__init__.py, tests/conftest.py, pytest.ini</files>
  <action>
1. Create tests/__init__.py (empty, marks as package)

2. Create tests/conftest.py with:
   - pytest-asyncio mode configuration (auto mode)
   - Fixture for test database (in-memory SQLite)
   - Fixture for sample place data
   - Fixture to reset database between tests

3. Create pytest.ini with:
   - testpaths = tests
   - asyncio_mode = auto
   - python_files = test_*.py
   - python_functions = test_*

Follow existing conventions: snake_case, type hints, minimal docstrings.
  </action>
  <verify>pytest --collect-only shows 0 tests collected (no errors)</verify>
  <done>tests/ directory exists, pytest runs without errors, conftest.py has db fixtures</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -r requirements-dev.txt` succeeds
- [ ] `pytest --collect-only` runs without errors
- [ ] tests/conftest.py has test_db and sample_place fixtures
- [ ] pytest.ini configures asyncio_mode = auto
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No import errors when running pytest
- Ready for test files in subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-testing-foundation/01-01-SUMMARY.md`
</output>
