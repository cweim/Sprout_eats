---
phase: 15-polish
plan: 01
type: execute
---

<objective>
Add Leaflet marker clustering for dense place collections.

Purpose: When users have 50+ places, markers overlap and become unreadable. Clustering groups nearby markers with count badge.
Output: Markers cluster at low zoom, expand on click/zoom.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior work:**
@.planning/phases/14-medium-priority-ux/14-04-SUMMARY.md

**Key files:**
@webapp/app.js - Map initialization at line ~301, markersLayer at line ~318
@webapp/index.html - Leaflet CDN includes
@webapp/styles.css - Map styling

**Tech available:**
- Leaflet.js loaded via CDN
- L.featureGroup for markers
- Custom marker icons (visited/unvisited)

**Pattern:**
Current marker setup uses L.featureGroup. Leaflet.markercluster is drop-in replacement that extends L.featureGroup.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Leaflet.markercluster CDN</name>
  <files>webapp/index.html</files>
  <action>
Add Leaflet.markercluster CSS and JS after existing Leaflet includes:
```html
<link rel="stylesheet" href="https://unpkg.com/leaflet.markercluster@1.4.1/dist/MarkerCluster.css">
<link rel="stylesheet" href="https://unpkg.com/leaflet.markercluster@1.4.1/dist/MarkerCluster.Default.css">
<script src="https://unpkg.com/leaflet.markercluster@1.4.1/dist/leaflet.markercluster.js"></script>
```
  </action>
  <verify>Open webapp in browser, check Network tab shows markercluster files loading</verify>
  <done>Leaflet.markercluster library loaded without errors</done>
</task>

<task type="auto">
  <name>Task 2: Replace featureGroup with markerClusterGroup</name>
  <files>webapp/app.js</files>
  <action>
1. Change markersLayer initialization from `L.featureGroup()` to `L.markerClusterGroup()`:
```javascript
markersLayer = L.markerClusterGroup({
    showCoverageOnHover: false,
    maxClusterRadius: 50,
    spiderfyOnMaxZoom: true,
    disableClusteringAtZoom: 16
}).addTo(map);
```

2. The rest of the code (addLayer, clearLayers, eachLayer) works unchanged because markerClusterGroup extends featureGroup.

3. Add custom cluster icon styling in styles.css to match app theme:
```css
.marker-cluster {
    background-color: var(--sprout-purple);
    opacity: 0.9;
}
.marker-cluster div {
    background-color: var(--sprout-green);
    color: var(--bg-color);
    font-weight: 600;
}
.marker-cluster-small {
    background-color: rgba(165, 153, 217, 0.6);
}
.marker-cluster-small div {
    background-color: rgba(165, 153, 217, 0.9);
}
.marker-cluster-medium {
    background-color: rgba(143, 212, 162, 0.6);
}
.marker-cluster-medium div {
    background-color: rgba(143, 212, 162, 0.9);
}
.marker-cluster-large {
    background-color: rgba(255, 107, 107, 0.6);
}
.marker-cluster-large div {
    background-color: rgba(255, 107, 107, 0.9);
}
```
  </action>
  <verify>Load app with 10+ places, zoom out until markers cluster, click cluster to expand</verify>
  <done>Markers cluster at low zoom, show count, expand on click</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Markercluster library loads without console errors
- [ ] Markers cluster when zoomed out (radius ~50px)
- [ ] Cluster shows place count
- [ ] Click cluster expands to show individual markers
- [ ] Zoom level 16+ shows all markers unclustered
- [ ] Existing marker functionality (popups, tooltips) still works
- [ ] Dark mode cluster colors work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Markers cluster smoothly on dense collections
- No regressions in existing map features
</success_criteria>

<output>
After completion, create `.planning/phases/15-polish/15-01-SUMMARY.md`
</output>
