---
phase: 06-interactive-viewer
plan: 04
type: execute
---

<objective>
Add visited toggle and personal notes features with database persistence.

Purpose: Let users track which places they've visited and add personal notes for future reference.
Output: Working visited toggle and notes input with API persistence, visible in both map and list views.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-interactive-viewer/DISCOVERY.md

**Prior plans:**
@.planning/phases/06-interactive-viewer/06-01-SUMMARY.md
@.planning/phases/06-interactive-viewer/06-02-SUMMARY.md
@.planning/phases/06-interactive-viewer/06-03-SUMMARY.md

**Source files:**
@database/models.py
@database/repository.py
@api/routes.py
@webapp/app.js
@webapp/styles.css

**Design constraint:**
- Cute, friendly UI
- Changes should feel satisfying (micro-interactions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add is_visited and notes columns to database</name>
  <files>database/models.py, database/repository.py</files>
  <action>
1. Update database/models.py Place class:
   - Add is_visited = Column(Boolean, default=False)
   - Add notes = Column(String, nullable=True)
   - Import Boolean from sqlalchemy

2. Update database/repository.py:
   - Add update_place(place_id, **kwargs) function
   - Accepts is_visited and/or notes as kwargs
   - Returns updated Place or None if not found
   - Use session.query().filter_by(id=place_id).first()
   - Update fields and commit

3. Note: SQLite will add columns on next migration or table recreation.
   For existing databases, user may need to delete and recreate.
   (Or add Alembic migration later - out of scope for this phase)
  </action>
  <verify>python -c "from database.models import Place; print(hasattr(Place, 'is_visited'), hasattr(Place, 'notes'))"</verify>
  <done>Place model has is_visited and notes columns, repository has update_place function</done>
</task>

<task type="auto">
  <name>Task 2: Add PATCH endpoint for updating places</name>
  <files>api/routes.py</files>
  <action>
1. Add PATCH /api/places/{place_id} endpoint:
   - Accept JSON body: { "is_visited": bool, "notes": string }
   - Both fields optional (partial update)
   - Call repository.update_place(place_id, **data)
   - Return updated place as JSON
   - Return 404 if place not found

2. Add Pydantic model for request validation:
   - PlaceUpdate with Optional[bool] is_visited, Optional[str] notes
   - Import from pydantic import BaseModel

3. Ensure response includes all place fields (same as GET /api/places format)
  </action>
  <verify>grep -n "PATCH" api/routes.py && grep -n "PlaceUpdate" api/routes.py</verify>
  <done>PATCH endpoint accepts is_visited and notes updates, returns updated place</done>
</task>

<task type="auto">
  <name>Task 3: Add visited toggle and notes UI to Mini App</name>
  <files>webapp/app.js, webapp/styles.css, webapp/index.html</files>
  <action>
1. Update popup content (map view):
   - Add visited toggle checkbox/button: "✓ Visited" or "Mark as visited"
   - Show notes preview if exists (truncated)
   - "Add note" button opens notes editor

2. Update place cards (list view):
   - Show visited badge: "✓ Visited" with checkmark
   - Different styling for visited places (subtle green tint or checkmark icon)
   - Notes indicator if notes exist
   - Tap notes area to edit

3. Create notes editor modal:
   - Simple overlay/modal with textarea
   - "Save" and "Cancel" buttons
   - Character limit hint (500 chars)
   - Auto-save on close or save button

4. Add API update functions:
   - updatePlaceVisited(placeId, isVisited) - PATCH call
   - updatePlaceNotes(placeId, notes) - PATCH call
   - Update local state after successful API call
   - Show brief "Saved!" feedback

5. Style interactions:
   - Toggle animation (satisfying click)
   - Visited places could have checkmark overlay on map markers
   - Use Telegram haptic feedback if available: Telegram.WebApp.HapticFeedback.impactOccurred('light')
  </action>
  <verify>Check app.js has updatePlaceVisited and updatePlaceNotes functions, CSS has .visited styling</verify>
  <done>Visited toggle and notes work in both views, changes persist via API</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Place model has is_visited and notes columns
- [ ] repository.update_place() function exists
- [ ] PATCH /api/places/{id} endpoint works
- [ ] Visited toggle updates database
- [ ] Notes editor saves to database
- [ ] UI reflects visited status (badge, styling)
- [ ] Notes visible in popup/card
- [ ] All 50 existing tests still pass
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Visited toggle persists across sessions
- Notes save and display correctly
- UI feedback is satisfying
- Phase 6 complete: Interactive Viewer shipped
</success_criteria>

<output>
After completion, create `.planning/phases/06-interactive-viewer/06-04-SUMMARY.md`

Final summary should note:
- Database schema updated for visited/notes
- API endpoint for partial updates
- Full interactive viewer complete
- Phase 6 complete, ready for Phase 7 (Proximity Features)
</output>
