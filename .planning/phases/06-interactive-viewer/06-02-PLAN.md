---
phase: 06-interactive-viewer
plan: 02
type: execute
---

<objective>
Implement interactive map view with Leaflet.js showing all saved places.

Purpose: Let users visually explore their saved places on a map with clickable markers.
Output: Working map view with markers, popups showing place details, and links to Google Maps/original reel.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-interactive-viewer/DISCOVERY.md

**Prior plan:**
@.planning/phases/06-interactive-viewer/06-01-SUMMARY.md

**Source files:**
@webapp/index.html
@webapp/styles.css
@webapp/app.js

**From DISCOVERY.md:**
- Leaflet.js from CDN (already included in 06-01)
- OpenStreetMap tiles (free, no API key)
- Markers with popups for each place

**Design constraint:**
- Cute, friendly UI with rounded corners and soft styling
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Leaflet map with place markers</name>
  <files>webapp/app.js, webapp/index.html</files>
  <action>
1. Update webapp/app.js:
   - Add initMap() function that creates Leaflet map
   - Use OpenStreetMap tiles: 'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png'
   - Set initial view to first place's coordinates (or default to [0, 0] if no places)
   - Fit map bounds to show all markers

2. Add fetchAndDisplayPlaces() function:
   - Fetch from API_URL + '/api/places' (API_URL configurable at top)
   - For each place, create a marker at [latitude, longitude]
   - Store markers in a LayerGroup for easy management
   - Call this after map initialization

3. Update webapp/index.html:
   - Ensure #map div has proper height (use CSS)
   - Map should fill available space below header

4. Handle empty state:
   - If no places, show friendly message: "No places saved yet! Send a video link to the bot to get started."
   - Center map on a default location (e.g., [0, 0] zoomed out)
  </action>
  <verify>Open webapp/index.html, check map initializes (will show default view without API)</verify>
  <done>Leaflet map displays with OpenStreetMap tiles, markers show for each place</done>
</task>

<task type="auto">
  <name>Task 2: Add rich popups with place details and action links</name>
  <files>webapp/app.js, webapp/styles.css</files>
  <action>
1. Create createPopupContent(place) function in app.js:
   - Place name (bold, larger)
   - Address
   - Rating with stars if available (e.g., "⭐ 4.5/5 (123 reviews)")
   - Types/categories (first 2, title case)
   - "Open in Google Maps" link: https://www.google.com/maps/place/?q=place_id:{google_place_id}
   - "Watch Original" link: source_url (opens original reel)
   - Both links open in new tab/external browser

2. Bind popup to each marker:
   - marker.bindPopup(createPopupContent(place))
   - Use custom popup options: maxWidth: 280, className: 'place-popup'

3. Style popups in webapp/styles.css:
   - .place-popup class for custom styling
   - Rounded corners, cute typography
   - Action buttons with friendly styling (not plain links)
   - Use Telegram theme colors

4. Add custom marker icons (optional but nice):
   - Use Leaflet's default markers for now (blue pins)
   - Could use different colors based on place_types later
  </action>
  <verify>Check popup HTML structure in app.js, verify CSS includes .place-popup styling</verify>
  <done>Clicking markers shows rich popup with place info, Google Maps link, and original reel link</done>
</task>

<task type="auto">
  <name>Task 3: Add map controls and user location</name>
  <files>webapp/app.js, webapp/styles.css</files>
  <action>
1. Add "My Location" button:
   - Create custom Leaflet control or simple button
   - On click, use navigator.geolocation.getCurrentPosition()
   - Pan map to user's location
   - Show user location marker (different style from place markers)
   - Handle permission denied gracefully

2. Add "Fit All" button:
   - Resets map view to show all saved places
   - Use map.fitBounds(markersLayer.getBounds())

3. Style control buttons:
   - Position in top-right or bottom-right
   - Cute, rounded button styling
   - Icons: 📍 for location, 🗺️ for fit all

4. Handle geolocation errors:
   - Show friendly toast/message: "Couldn't get your location. Make sure location is enabled!"
   - Don't break the app if geolocation fails
  </action>
  <verify>Check app.js has geolocation code, verify control buttons are styled</verify>
  <done>Map has location button and fit-all button, user location works with permission</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Map displays with OpenStreetMap tiles
- [ ] Markers appear at correct coordinates (test with mock data if needed)
- [ ] Popup shows place name, address, rating, links
- [ ] "Open in Google Maps" link format is correct
- [ ] "Watch Original" link uses source_url
- [ ] Location button requests geolocation permission
- [ ] Fit All button resets map bounds
- [ ] Styling is cute and uses Telegram theme colors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Map is interactive and responsive
- Popups provide useful place information
- Links work correctly
- Ready for 06-03 (List View)
</success_criteria>

<output>
After completion, create `.planning/phases/06-interactive-viewer/06-02-SUMMARY.md`

Summary should note:
- Leaflet map with OpenStreetMap tiles
- Markers with rich popups
- Google Maps and original reel links
- Location and fit-all controls
- Ready for 06-03 (List View)
</output>
