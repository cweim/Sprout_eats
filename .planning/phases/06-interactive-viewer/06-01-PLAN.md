---
phase: 06-interactive-viewer
plan: 01
type: execute
---

<objective>
Set up Mini App infrastructure: FastAPI backend, HTML skeleton, and bot integration.

Purpose: Create the foundation for the interactive viewer - API to serve places, web frontend skeleton, and bot button to launch it.
Output: Working Mini App that opens from bot and displays "Hello" with Telegram theme.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-interactive-viewer/DISCOVERY.md

**Source files:**
@database/repository.py
@database/models.py
@bot/handlers.py
@config.py
@requirements.txt

**From DISCOVERY.md:**
- FastAPI for API backend
- Vanilla HTML/CSS/JS for Mini App
- Telegram WebApp API for integration
- GitHub Pages for hosting (user will deploy manually)

**Design constraint from PROJECT.md:**
- Cute, friendly UI design
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FastAPI and create places API endpoint</name>
  <files>requirements.txt, api/__init__.py, api/routes.py, api/main.py</files>
  <action>
1. Add to requirements.txt:
   - fastapi>=0.109.0
   - uvicorn>=0.27.0

2. Create api/__init__.py (empty)

3. Create api/routes.py with:
   - GET /api/places - Returns all places as JSON
   - Use repository.get_all_places()
   - Convert Place objects to dict with all fields
   - Include CORS headers for Mini App access

4. Create api/main.py with:
   - FastAPI app instance
   - CORS middleware (allow all origins for development)
   - Include routes from api/routes.py
   - Health check endpoint GET /api/health

Response format for places:
```json
{
  "places": [
    {
      "id": 1,
      "name": "...",
      "address": "...",
      "latitude": 0.0,
      "longitude": 0.0,
      "source_url": "...",
      "place_types": "restaurant,food",
      "place_rating": 4.5,
      ...
    }
  ]
}
```
  </action>
  <verify>pip install -r requirements.txt && python -c "from api.main import app; print('API OK')"</verify>
  <done>FastAPI app created with /api/places and /api/health endpoints, CORS enabled</done>
</task>

<task type="auto">
  <name>Task 2: Create Mini App HTML skeleton with Telegram integration</name>
  <files>webapp/index.html, webapp/styles.css, webapp/app.js</files>
  <action>
1. Create webapp/ directory

2. Create webapp/index.html:
   - Include Telegram WebApp script: https://telegram.org/js/telegram-web-app.js
   - Include Leaflet CSS/JS from CDN (for next plan)
   - Link to styles.css and app.js
   - Basic structure: header, map container, list container
   - Call Telegram.WebApp.ready() on load
   - Use Telegram theme CSS variables for colors

3. Create webapp/styles.css:
   - Use Telegram theme variables: var(--tg-theme-bg-color), var(--tg-theme-text-color), etc.
   - Mobile-first responsive design
   - Cute, friendly styling (rounded corners, soft shadows)
   - Full viewport height using var(--tg-viewport-height)

4. Create webapp/app.js:
   - Initialize Telegram WebApp
   - Log user info from Telegram.WebApp.initDataUnsafe
   - Placeholder function to fetch places from API
   - For now, display "Loading places..." message

Keep it minimal - map and list views come in later plans.
  </action>
  <verify>Open webapp/index.html in browser (shows basic page, console logs WebApp not available outside Telegram)</verify>
  <done>Mini App skeleton created with Telegram WebApp integration, cute styling, responsive layout</done>
</task>

<task type="auto">
  <name>Task 3: Add Mini App launch button to bot</name>
  <files>bot/handlers.py, config.py</files>
  <action>
1. Add to config.py:
   - WEBAPP_URL = os.getenv("WEBAPP_URL", "")
   - This will be the GitHub Pages URL (or localhost for testing)

2. Update bot/handlers.py:
   - Import WebAppInfo from telegram
   - Add "Open Viewer" button to start_command menu keyboard
   - Use InlineKeyboardButton with web_app=WebAppInfo(url=config.WEBAPP_URL)
   - Only show button if WEBAPP_URL is configured (non-empty)

3. Add /viewer command as alternative way to open:
   - async def viewer_command(update, context)
   - Send message with single "Open Viewer" button
   - Friendly message: "Tap below to explore your saved places!"

Note: The Mini App won't fully work until deployed to HTTPS (GitHub Pages). For local testing, users can open webapp/index.html directly in browser.
  </action>
  <verify>grep -n "WebAppInfo" bot/handlers.py && grep -n "WEBAPP_URL" config.py</verify>
  <done>Bot has "Open Viewer" button in menu, /viewer command added, WEBAPP_URL configurable</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -r requirements.txt` installs FastAPI and uvicorn
- [ ] `python -c "from api.main import app"` imports successfully
- [ ] webapp/index.html exists with Telegram WebApp script included
- [ ] webapp/styles.css uses Telegram theme variables
- [ ] bot/handlers.py has WebAppInfo import and viewer button
- [ ] All 50 existing tests still pass
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors introduced
- API endpoint returns places JSON
- Mini App skeleton ready for map/list implementation
- Bot can launch Mini App (when WEBAPP_URL configured)
</success_criteria>

<output>
After completion, create `.planning/phases/06-interactive-viewer/06-01-SUMMARY.md`

Summary should note:
- FastAPI backend with /api/places endpoint
- Mini App skeleton with Telegram theming
- Bot integration with viewer launch button
- Ready for 06-02 (Map View)
</output>
