---
phase: 06-interactive-viewer
plan: 03
type: execute
---

<objective>
Implement list view with category grouping, search, and filtering.

Purpose: Provide an alternative way to browse places, especially useful when looking for specific types or searching by name.
Output: Swipeable list view with place cards, search bar, category filters, and sort options.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-interactive-viewer/DISCOVERY.md

**Prior plans:**
@.planning/phases/06-interactive-viewer/06-01-SUMMARY.md
@.planning/phases/06-interactive-viewer/06-02-SUMMARY.md

**Source files:**
@webapp/index.html
@webapp/styles.css
@webapp/app.js

**Design constraint:**
- Cute, friendly UI
- Mobile-first (Telegram Mini App context)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add view toggle and list container</name>
  <files>webapp/index.html, webapp/styles.css, webapp/app.js</files>
  <action>
1. Update webapp/index.html:
   - Add view toggle buttons in header: "Map" and "List" (or icons 🗺️ / 📋)
   - Add #list-view container (initially hidden)
   - Add search input field at top of list view
   - Add filter chips area below search

2. Update webapp/styles.css:
   - View toggle button styling (active state indicator)
   - List view container styling
   - Hide/show classes for switching views
   - Search input styling (rounded, cute)
   - Filter chips styling (pill-shaped, selectable)

3. Update webapp/app.js:
   - Add view state: 'map' or 'list'
   - Toggle functions to switch views
   - Show/hide appropriate containers
   - Remember view preference (optional: localStorage)
  </action>
  <verify>Toggle between map and list views in browser</verify>
  <done>View toggle works, list container shows when selected, search input visible</done>
</task>

<task type="auto">
  <name>Task 2: Render place cards in list view</name>
  <files>webapp/app.js, webapp/styles.css</files>
  <action>
1. Create renderPlaceCard(place) function:
   - Card with place name (bold)
   - Address (smaller, muted color)
   - Rating with stars if available
   - Category chips (first 2 types)
   - Source platform icon (Instagram/TikTok)
   - Tap card to show on map (switch view + pan to marker)

2. Create renderPlacesList(places) function:
   - Clear existing list
   - For each place, create and append card
   - Group by category if filter active (later)
   - Show count: "Showing X places"

3. Style place cards in CSS:
   - Card layout with padding, shadow, rounded corners
   - Cute, friendly appearance
   - Touch-friendly size (min 48px touch targets)
   - Category chips with background colors
   - Platform icons (could use emoji: 📸 for Instagram, 🎵 for TikTok)

4. Add "tap to view on map" interaction:
   - Clicking card switches to map view
   - Pans to that place's marker
   - Opens the marker's popup
  </action>
  <verify>List view shows place cards with correct information</verify>
  <done>Place cards display in list view with all metadata, tapping shows place on map</done>
</task>

<task type="auto">
  <name>Task 3: Implement search and category filters</name>
  <files>webapp/app.js, webapp/styles.css</files>
  <action>
1. Implement search functionality:
   - Filter places by name or address (case-insensitive contains)
   - Update list on input (debounce 300ms for performance)
   - Show "No places match your search" for empty results
   - Clear button (X) to reset search

2. Implement category filter chips:
   - Extract unique categories from all places (first type of each)
   - Display as selectable chips: "Restaurant", "Cafe", "Bar", etc.
   - Multiple selection allowed
   - "All" chip to clear filters
   - Filter list to show only matching categories

3. Add sort options:
   - Dropdown or segmented control: "Newest", "Name A-Z", "Rating"
   - Default to "Newest" (by created_at)
   - Sort by rating shows highest first

4. Combine filters:
   - Search + category + sort all work together
   - Show result count: "Showing X of Y places"
  </action>
  <verify>Search filters list, category chips filter by type, sort changes order</verify>
  <done>Search, category filters, and sort all work together, results update in real-time</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] View toggle switches between map and list
- [ ] Place cards show name, address, rating, categories
- [ ] Search filters by name and address
- [ ] Category chips filter by place type
- [ ] Sort options work (Newest, Name, Rating)
- [ ] Tapping card shows place on map
- [ ] Empty states have friendly messages
- [ ] Mobile-friendly touch targets
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- List view is usable and responsive
- Search and filters work correctly
- Map/list integration smooth
- Ready for 06-04 (Visited & Notes)
</success_criteria>

<output>
After completion, create `.planning/phases/06-interactive-viewer/06-03-SUMMARY.md`

Summary should note:
- View toggle between map and list
- Place cards with rich metadata
- Search by name/address
- Category filter chips
- Sort options
- Ready for 06-04 (Visited & Notes)
</output>
