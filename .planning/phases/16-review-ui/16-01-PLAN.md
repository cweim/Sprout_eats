---
phase: 16-review-ui
plan: 01
type: execute
---

<objective>
Restructure review sheet layout for better hierarchy and space efficiency.

Purpose: Users rate overall experience first; current layout forces scrolling past dishes.
Output: Overall section first, compact header, rating+price inline.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REVIEW_UI_AUDIT.md

**Key files:**
@webapp/index.html - Review sheet HTML structure (lines 233-287)
@webapp/styles.css - Sheet styling, review sections, rating rows
@webapp/app.js - openReviewSheet, populateReview logic

**Current issues:**
- Title "Review: Place Name" redundant
- 3 buttons in header (share + view place + close)
- Dishes section before Overall
- Rating and Price on separate rows
- Overall section takes ~250px vertical

**Target layout:**
```
Header: "Review" title, place name as subtitle, close button only
Body:
  1. Overall Section (compact)
     - Rating ⭐⭐⭐⭐⭐  Price $$$$  (same row)
     - [photo] [photo] [photo] [+]
     - [Notes textarea]
  2. Dishes Section (unchanged for now)
Footer: [Delete] [Share] [Save]
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure header</name>
  <files>webapp/index.html, webapp/styles.css</files>
  <action>
1. Update review sheet header HTML:
```html
<div class="sheet-header">
    <div class="sheet-handle"></div>
    <div class="sheet-title-group">
        <h2 id="review-sheet-title">Review</h2>
        <span id="review-sheet-place" class="sheet-subtitle"></span>
    </div>
    <button id="review-sheet-close" class="sheet-close-btn" aria-label="Close">×</button>
</div>
```

2. Move share button to footer (next to save).

3. Update CSS:
```css
.sheet-title-group {
    text-align: center;
    flex: 1;
}
.sheet-subtitle {
    display: block;
    font-size: 13px;
    color: var(--hint-color);
    font-weight: 400;
    margin-top: 2px;
}
```

4. Update openReviewSheet() to set subtitle:
```javascript
document.getElementById('review-sheet-title').textContent = 'Review';
document.getElementById('review-sheet-place').textContent = place.name;
```

5. Remove dynamic "View Place" button creation - use existing view toggle instead.
  </action>
  <verify>Header shows "Review" with place name as subtitle, only close button visible</verify>
  <done>Header cleaned up with subtitle</done>
</task>

<task type="auto">
  <name>Task 2: Flip Overall before Dishes</name>
  <files>webapp/index.html</files>
  <action>
1. In index.html, swap the order of review sections:
```html
<div class="sheet-body" id="review-sheet-body">
    <!-- Overall Section (FIRST) -->
    <div class="review-section">
        <div class="section-header">Overall</div>
        ...
    </div>

    <!-- Dishes Section (SECOND) -->
    <div class="review-section">
        <div class="section-header">Dishes</div>
        ...
    </div>
</div>
```
  </action>
  <verify>Overall section appears first when opening review sheet</verify>
  <done>Section order flipped</done>
</task>

<task type="auto">
  <name>Task 3: Compact overall ratings (same row)</name>
  <files>webapp/index.html, webapp/styles.css</files>
  <action>
1. Combine rating and price into single row:
```html
<div class="overall-ratings-row">
    <div class="rating-group">
        <label>Rating</label>
        <div id="overall-stars" class="star-rating" data-rating="0"></div>
    </div>
    <div class="rating-group">
        <label>Price</label>
        <div id="price-rating" class="price-rating" data-rating="0"></div>
    </div>
</div>
```

2. Add CSS:
```css
.overall-ratings-row {
    display: flex;
    gap: 24px;
    margin-bottom: 16px;
}
.rating-group {
    display: flex;
    align-items: center;
    gap: 8px;
}
.rating-group label {
    font-size: 13px;
    font-weight: 500;
    color: var(--hint-color);
}
```

3. Use smaller stars (24px) for overall to match dishes:
```css
.overall-ratings-row .star-rating .star {
    width: 24px;
    height: 24px;
    font-size: 18px;
}
.overall-ratings-row .price-icon {
    width: 22px;
    height: 22px;
    font-size: 16px;
}
```

4. Remove old .overall-rating-row styles or mark deprecated.
  </action>
  <verify>Rating and Price appear side by side, stars are smaller</verify>
  <done>Compact rating row implemented</done>
</task>

<task type="auto">
  <name>Task 4: Move share to footer</name>
  <files>webapp/index.html, webapp/styles.css, webapp/app.js</files>
  <action>
1. Add share button to footer:
```html
<div class="sheet-footer">
    <button id="delete-review-btn" class="sheet-btn danger" style="display: none;">Delete</button>
    <button id="share-review-btn" class="sheet-btn secondary" style="display: none;">Share</button>
    <button id="save-review-btn" class="sheet-btn primary">Save</button>
</div>
```

2. Add secondary button style:
```css
.sheet-btn.secondary {
    background: var(--secondary-bg);
    color: var(--text-color);
}
.sheet-btn.secondary:hover {
    background: var(--sprout-purple-light);
    color: var(--sprout-purple);
}
```

3. Remove share button from header (the SVG icon button).

4. Update button visibility logic in openReviewSheet() - show share for existing reviews.
  </action>
  <verify>Share button appears in footer for existing reviews</verify>
  <done>Share moved to footer</done>
</task>

<task type="auto">
  <name>Task 5: Remove photo section label</name>
  <files>webapp/index.html, webapp/styles.css</files>
  <action>
1. Remove the label "Place Photos (ambiance, etc.)" - photos are self-explanatory.

2. Keep just the photo grid:
```html
<div id="overall-photos" class="photo-grid"></div>
```

3. Remove .section-sublabel styles if unused elsewhere.
  </action>
  <verify>Photo grid visible without label above it</verify>
  <done>Photo label removed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Header shows "Review" title with place name subtitle
- [ ] Only close button in header (no share icon)
- [ ] Overall section appears before Dishes
- [ ] Rating and Price on same row
- [ ] Stars are 24px (smaller than before)
- [ ] Share button in footer (for existing reviews)
- [ ] No "Place Photos" label
- [ ] Works from List view, Map view, and Reviews tab
- [ ] Dark mode works
- [ ] No console errors
</verification>

<success_criteria>
- All tasks completed
- Review sheet feels more compact and organized
- Overall rating is immediately visible without scrolling
</success_criteria>

<output>
After completion, create `.planning/phases/16-review-ui/16-01-SUMMARY.md`
</output>
