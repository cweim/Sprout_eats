---
phase: 04-enhanced-metadata
plan: 03
type: execute
---

<objective>
Update bot handlers to display enhanced metadata in place confirmations and listings.

Purpose: Show users the rich metadata we now capture, making the bot more informative and delightful.
Output: Enhanced place save messages with ratings/categories, improved /places listing with metadata.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior plans:**
@.planning/phases/04-enhanced-metadata/04-01-SUMMARY.md
@.planning/phases/04-enhanced-metadata/04-02-SUMMARY.md

**Source files to modify:**
@bot/handlers.py

**Design constraints from PROJECT.md:**
- Bot personality: Cute, friendly, encouraging tone
- Celebrate user actions (place saved, etc.)

**New data available:**
- From video: source_title, source_uploader, source_duration, source_hashtags
- From place: place_types, place_rating, place_rating_count, place_price_level, place_opening_hours

**Established patterns:** InlineKeyboardButton, reply_text with emoji, message formatting
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update handle_url to save and display enhanced metadata</name>
  <files>bot/handlers.py</files>
  <action>
1. In handle_url(), after getting DownloadResult, extract video metadata:
   - source_title = result.title
   - source_uploader = result.uploader
   - source_duration = result.duration
   - source_hashtags = ",".join(result.hashtags) if result.hashtags else None

2. When calling repository.add_place() (both single and multi-place paths), pass:
   - source_title=source_title
   - source_uploader=source_uploader
   - source_duration=source_duration
   - source_hashtags=source_hashtags
   - place_types=",".join(place.types) if place.types else None
   - place_rating=place.rating
   - place_rating_count=place.rating_count
   - place_price_level=place.price_level
   - place_opening_hours=place.opening_hours

3. Update confirmation message to show rich info:
   ```
   Found and saved: {name}! 🎉
   📍 {address}
   ⭐ {rating}/5 ({rating_count} reviews)  [if rating exists]
   🏷️ {types}  [if types exist, e.g., "Restaurant, Cafe"]
   📺 From @{uploader}  [if uploader exists]
   ```

4. Also update select_place_callback() similarly to pass metadata when saving.

Keep messages concise - only show fields that have values. Use title case for types.
  </action>
  <verify>Manual test: Send URL, verify confirmation shows rating/type/uploader when available</verify>
  <done>Place save messages include rating, types, and source info when available</done>
</task>

<task type="auto">
  <name>Task 2: Update places_command and action_places to show metadata</name>
  <files>bot/handlers.py</files>
  <action>
1. Update places_command() to show richer listing:
   ```
   📍 Saved places ({count}):

   1. {name}
      {address}
      ⭐ {rating}/5 • {types}  [if available]

   2. {name}
      ...
   ```

2. Update action_callback() for "action_places" with same format.

3. Helper function format_place_line(place) to avoid duplication:
   - Returns formatted string with name, address, optional rating/types
   - Use title case for types: "restaurant" -> "Restaurant"
   - Limit types to first 2: ["restaurant", "cafe", "food"] -> "Restaurant, Cafe"

Keep it readable - don't overwhelm with data. Rating and primary type are most useful.
  </action>
  <verify>Run /places command, verify listing shows ratings and categories for places that have them</verify>
  <done>/places shows enhanced metadata; action_places callback shows same format</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/ -v` passes ALL tests
- [ ] Place save confirmation shows rating/types when available
- [ ] /places listing shows rating/types for each place
- [ ] Messages remain friendly and not cluttered
- [ ] Empty metadata fields don't show placeholder text
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users see rich metadata in save confirmations
- Place listings include ratings and categories
- Phase 4 complete: Enhanced Metadata shipped
</success_criteria>

<output>
After completion, create `.planning/phases/04-enhanced-metadata/04-03-SUMMARY.md`

Final summary should note:
- New message formats
- Metadata fields displayed
- Phase 4 complete, ready for Phase 5 (Language Support)
</output>
