---
phase: 04-enhanced-metadata
plan: 01
type: execute
---

<objective>
Add video metadata fields to database and extract additional yt-dlp fields.

Purpose: Capture richer context from source videos (uploader, duration, hashtags) to help users remember where they found places.
Output: Updated Place model with video metadata fields, enhanced DownloadResult with additional yt-dlp data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior plan:**
@.planning/phases/03-multi-place-support/03-02-SUMMARY.md

**Source files to modify:**
@database/models.py
@services/downloader.py
@database/repository.py

**Tech stack available:** SQLAlchemy, yt-dlp, dataclasses
**Established patterns:** SQLAlchemy Column definitions, DownloadResult dataclass, repository functions

**yt-dlp available fields:** title, description, uploader, uploader_id, duration, view_count, like_count, timestamp, hashtags (as list)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add video metadata fields to Place model</name>
  <files>database/models.py, database/repository.py</files>
  <action>
1. Add new columns to Place model in models.py:
   - source_title: String (video title, may differ from place name)
   - source_uploader: String (creator username)
   - source_duration: Integer (video duration in seconds)
   - source_hashtags: String (comma-separated hashtags)

2. Update repository.add_place() to accept new optional parameters:
   - source_title: Optional[str] = None
   - source_uploader: Optional[str] = None
   - source_duration: Optional[int] = None
   - source_hashtags: Optional[str] = None

3. SQLite will auto-migrate with new nullable columns. No explicit migration needed.
  </action>
  <verify>python -c "from database.models import Place; print([c.name for c in Place.__table__.columns])" shows new columns</verify>
  <done>Place model has source_title, source_uploader, source_duration, source_hashtags columns; repository accepts these parameters</done>
</task>

<task type="auto">
  <name>Task 2: Extract additional yt-dlp metadata in downloader</name>
  <files>services/downloader.py</files>
  <action>
1. Add new fields to DownloadResult dataclass:
   - uploader: Optional[str] = None
   - duration: Optional[int] = None
   - hashtags: list[str] = field(default_factory=list)

2. In download_content(), extract from info dict:
   - uploader = info.get("uploader") or info.get("uploader_id")
   - duration = info.get("duration")  # Already an int in seconds
   - hashtags = info.get("tags", []) or []  # yt-dlp uses "tags" not "hashtags"

3. Return these in DownloadResult.

Note: Use field(default_factory=list) for hashtags to avoid mutable default argument issue.
  </action>
  <verify>pytest tests/test_downloader.py -v passes; manual inspection of DownloadResult shows new fields</verify>
  <done>DownloadResult includes uploader, duration, hashtags; download_content extracts these from yt-dlp info dict</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/ -v` passes ALL tests (no regressions)
- [ ] Place model has 4 new columns
- [ ] DownloadResult has 3 new fields
- [ ] repository.add_place() accepts new parameters
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Database schema extended with video metadata
- Downloader extracts richer metadata from yt-dlp
</success_criteria>

<output>
After completion, create `.planning/phases/04-enhanced-metadata/04-01-SUMMARY.md`

Summary should note:
- New Place model columns
- New DownloadResult fields
- yt-dlp field mappings used
- Ready for 04-02 (Places API enhanced fields)
</output>
