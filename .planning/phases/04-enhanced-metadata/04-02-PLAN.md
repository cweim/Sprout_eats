---
phase: 04-enhanced-metadata
plan: 02
type: execute
---

<objective>
Expand Places API fields and add place metadata to database.

Purpose: Capture richer place data (categories, ratings, photos, hours) to help users evaluate and remember places.
Output: Enhanced PlaceResult with metadata, updated Place model with place-specific fields, repository storing this data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior plan:**
@.planning/phases/04-enhanced-metadata/04-01-SUMMARY.md

**Source files to modify:**
@services/places.py
@database/models.py
@database/repository.py

**Google Places API fields available:**
- places.types (list of category strings like "restaurant", "cafe")
- places.rating (float 1.0-5.0)
- places.userRatingCount (int)
- places.regularOpeningHours.weekdayDescriptions (list of strings)
- places.photos (list with photo references - requires separate API call to fetch)
- places.priceLevel (enum: PRICE_LEVEL_FREE, INEXPENSIVE, MODERATE, EXPENSIVE, VERY_EXPENSIVE)

**Current field mask:** places.displayName,places.formattedAddress,places.location,places.id
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Places API field mask and PlaceResult</name>
  <files>services/places.py</files>
  <action>
1. Update PlaceResult dataclass with new optional fields:
   - types: list[str] = field(default_factory=list)  # e.g., ["restaurant", "food"]
   - rating: Optional[float] = None  # 1.0-5.0
   - rating_count: Optional[int] = None
   - price_level: Optional[str] = None  # "FREE", "INEXPENSIVE", etc.
   - opening_hours: Optional[str] = None  # First line of weekday descriptions

2. Update X-Goog-FieldMask header to include:
   "places.displayName,places.formattedAddress,places.location,places.id,places.types,places.rating,places.userRatingCount,places.priceLevel,places.regularOpeningHours"

3. In search_place(), parse additional fields from response:
   - types = place.get("types", [])
   - rating = place.get("rating")
   - rating_count = place.get("userRatingCount")
   - price_level = place.get("priceLevel", "").replace("PRICE_LEVEL_", "") if present
   - opening_hours = first item of place.get("regularOpeningHours", {}).get("weekdayDescriptions", []) or None

Note: Skip photos for now - requires separate API call and adds complexity. Can be added in future phase.
  </action>
  <verify>pytest tests/test_places.py -v passes (may need test updates for new fields)</verify>
  <done>PlaceResult includes types, rating, rating_count, price_level, opening_hours; API requests expanded field mask</done>
</task>

<task type="auto">
  <name>Task 2: Add place metadata columns and update repository</name>
  <files>database/models.py, database/repository.py</files>
  <action>
1. Add new columns to Place model:
   - place_types: String (comma-separated types like "restaurant,food")
   - place_rating: Float (nullable)
   - place_rating_count: Integer (nullable)
   - place_price_level: String (nullable, e.g., "MODERATE")
   - place_opening_hours: String (nullable, e.g., "Monday: 9:00 AM – 10:00 PM")

2. Update repository.add_place() to accept new optional parameters:
   - place_types: Optional[str] = None
   - place_rating: Optional[float] = None
   - place_rating_count: Optional[int] = None
   - place_price_level: Optional[str] = None
   - place_opening_hours: Optional[str] = None

3. In add_place(), store the values when creating new Place instance.
  </action>
  <verify>python -c "from database.models import Place; print([c.name for c in Place.__table__.columns])" shows place metadata columns</verify>
  <done>Place model has place_types, place_rating, place_rating_count, place_price_level, place_opening_hours; repository stores these</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/ -v` passes ALL tests
- [ ] PlaceResult has 5 new metadata fields
- [ ] Place model has 5 new place metadata columns
- [ ] API field mask includes types, rating, hours, price
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Places API returns richer metadata
- Database stores place-specific metadata
</success_criteria>

<output>
After completion, create `.planning/phases/04-enhanced-metadata/04-02-SUMMARY.md`

Summary should note:
- New PlaceResult fields
- New Place model columns
- API field mask changes
- Ready for 04-03 (bot display updates)
</output>
