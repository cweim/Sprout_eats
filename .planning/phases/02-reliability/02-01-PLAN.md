---
phase: 02-reliability
plan: 01
type: execute
---

<objective>
Add retry logic with exponential backoff for Google API calls.

Purpose: Handle transient network failures gracefully without user intervention.
Output: services/places.py and services/maps.py with retry decorators, plus tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior phase:**
@.planning/phases/01-testing-foundation/01-03-SUMMARY.md (aioresponses mocking pattern)

**Source files to modify:**
@services/places.py
@services/maps.py

**Test infrastructure:**
@tests/conftest.py
@pytest.ini

**Tech stack available:** pytest, aioresponses, aiohttp
**Established patterns:** aioresponses context manager for HTTP mocking
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenacity retry to places service</name>
  <files>services/places.py, requirements.txt</files>
  <action>
1. Add tenacity to requirements.txt
2. In services/places.py, create a retry decorator using tenacity:
   - Retry on aiohttp.ClientError and asyncio.TimeoutError
   - Use exponential backoff: wait_exponential(multiplier=1, min=1, max=10)
   - Max 3 attempts with stop_after_attempt(3)
   - Log retries using before_sleep callback
3. Apply decorator to search_place() function
4. Add aiohttp.ClientTimeout(total=10) to the ClientSession

Keep the existing function signature unchanged. The retry is transparent to callers.
  </action>
  <verify>Manual inspection of retry decorator application. Unit tests in Task 2.</verify>
  <done>search_place() has retry decorator with exponential backoff, timeout configured</done>
</task>

<task type="auto">
  <name>Task 2: Add tenacity retry to maps service and write tests</name>
  <files>services/maps.py, tests/test_retry.py</files>
  <action>
1. In services/maps.py:
   - Import tenacity and add same retry decorator pattern as places.py
   - Apply to generate_map_image() function
   - Add aiohttp.ClientTimeout(total=10) to ClientSession

2. Create tests/test_retry.py to verify retry behavior:
   - TestPlacesRetry:
     - test_search_place_retries_on_network_error: Mock aioresponses to fail twice then succeed, verify 3 calls made
     - test_search_place_gives_up_after_max_retries: Mock to always fail, verify RetryError raised after 3 attempts
   - TestMapsRetry:
     - test_generate_map_retries_on_network_error: Same pattern as places
     - test_generate_map_gives_up_after_max_retries: Same pattern

Use aioresponses to simulate network failures by raising aiohttp.ClientError.
  </action>
  <verify>pytest tests/test_retry.py -v shows all 4 tests passing</verify>
  <done>maps.py has retry decorator, 4 retry tests passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -r requirements.txt` installs tenacity
- [ ] `pytest tests/test_retry.py -v` shows all tests passing
- [ ] `pytest tests/ -v` shows ALL project tests passing (no regressions)
- [ ] Both services have retry decorators with exponential backoff
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- tenacity added to requirements.txt
- Retry logic tested with mock failures
- No changes to public function signatures
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliability/02-01-SUMMARY.md`

Final summary should note:
- Retry configuration (3 attempts, exponential backoff 1-10s)
- Test count added
- Ready for 02-02 (download timeout)
</output>
