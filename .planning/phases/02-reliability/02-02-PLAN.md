---
phase: 02-reliability
plan: 02
type: execute
---

<objective>
Add download timeout protection and friendly error messages.

Purpose: Prevent hanging on large videos and maintain cute, friendly bot personality in errors.
Output: Timeout-protected downloads and user-friendly error handling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior plan:**
@.planning/phases/02-reliability/02-01-SUMMARY.md (retry logic)

**Source files to modify:**
@services/downloader.py
@bot/handlers.py

**Design constraint from PROJECT.md:**
- Bot personality: Cute, friendly, encouraging tone in all messages
- Error messages should be helpful, not scary
- Celebrate user actions

**Test infrastructure:**
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add download timeout to downloader service</name>
  <files>services/downloader.py, tests/test_downloader.py</files>
  <action>
1. In services/downloader.py:
   - Add DOWNLOAD_TIMEOUT = 120 constant (2 minutes, configurable)
   - Wrap the run_in_executor call with asyncio.timeout(DOWNLOAD_TIMEOUT)
   - Create a custom DownloadTimeoutError exception class
   - On timeout, raise DownloadTimeoutError with friendly message

2. Add test to tests/test_downloader.py:
   - TestDownloadTimeout class:
     - test_download_timeout_raises_error: Mock a slow download, verify DownloadTimeoutError raised

Use unittest.mock to patch the executor to simulate slow download.
Note: Testing actual timeout is tricky - focus on the exception class existing and being importable.
  </action>
  <verify>pytest tests/test_downloader.py -v shows all tests passing</verify>
  <done>download_content() has timeout protection, DownloadTimeoutError defined</done>
</task>

<task type="auto">
  <name>Task 2: Update error messages to friendly tone</name>
  <files>bot/handlers.py</files>
  <action>
Update error messages in bot/handlers.py to be cute and friendly per PROJECT.md personality:

1. In handle_url() exception handler (line ~272-274):
   - Change: "Error processing video: {str(e)}"
   - To context-aware friendly messages:
     - DownloadTimeoutError: "Oh no! This video is taking too long to download. Maybe try a shorter one?"
     - Network errors: "Hmm, I'm having trouble connecting right now. Give me a moment and try again!"
     - Generic: "Oops! Something went wrong while processing that video. Mind trying again?"

2. In handle_text() exception handler (line ~335-337):
   - Change: "Error searching: {str(e)}"
   - To: "I couldn't find that place. Could you try a different name or be more specific?"

3. In map_command() and action_callback map section:
   - Change: "Error generating map: {str(e)}"
   - To: "I had trouble making the map. Let me know if this keeps happening!"

4. Add import for DownloadTimeoutError at top of handlers.py

Keep logging the actual error for debugging, but show friendly message to user.
  </action>
  <verify>Review handlers.py for friendly error messages. Run pytest tests/ to ensure no regressions.</verify>
  <done>All user-facing error messages are friendly and encouraging</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/ -v` shows ALL project tests passing
- [ ] DownloadTimeoutError is defined and importable
- [ ] Error messages in handlers.py are friendly (grep for "Oops", "Oh no", etc.)
- [ ] Original errors still logged for debugging
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Download has 120s timeout protection
- Error messages match cute/friendly bot personality
- Phase 2 complete: Reliability improvements in place
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliability/02-02-SUMMARY.md`

Final summary should note:
- Timeout configuration (120s default)
- Error message improvements made
- Phase 2 complete, ready for Phase 3 (Multi-Place Support)
</output>
