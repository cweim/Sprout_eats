---
phase: 05-language-support
plan: 02
type: execute
---

<objective>
Integrate language-aware transcription into bot handlers for improved search accuracy.

Purpose: Use English translations for place search to improve Google Places API results, store language info for reference.
Output: Bot uses translated text for search, language metadata stored with places.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior plans:**
@.planning/phases/05-language-support/05-01-SUMMARY.md

**Source files to modify:**
@bot/handlers.py

**From 05-01:**
- TranscriptionResult has: text, language, english_text
- repository.add_place() accepts: source_language, source_transcript, source_transcript_en

**Design constraints from PROJECT.md:**
- Bot personality: Cute, friendly, encouraging tone
- Minimal friction, delightful feedback

**Current flow in handle_url():**
1. Download video
2. Try caption/title for search
3. If no places found, transcribe audio and search with transcript
4. Save place with metadata
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update handle_url to use TranscriptionResult</name>
  <files>bot/handlers.py</files>
  <action>
1. Update the transcription section in handle_url():
   - transcription_result = await transcribe_audio(result.audio_path)
   - Use transcription_result.english_text or transcription_result.text for search
   - Prefer english_text if available (better for Google Places API)

2. Store language info when saving place:
   - source_language = transcription_result.language
   - source_transcript = transcription_result.text
   - source_transcript_en = transcription_result.english_text

3. Update the "no location found" message to show detected language:
   - "Could not find a location (detected: {language})"
   - Use friendly language names: "Japanese" not "ja"

4. Pass language params to repository.add_place() in both single and multi-place paths

Language code to name mapping (simple dict):
{"en": "English", "ja": "Japanese", "ko": "Korean", "zh": "Chinese", "es": "Spanish", "fr": "French", ...}
Fall back to code if not in dict.
  </action>
  <verify>Manual test: Send non-English video, verify language detected and used for search</verify>
  <done>handle_url uses english_text for search, stores language metadata in database</done>
</task>

<task type="auto">
  <name>Task 2: Update select_place_callback for language metadata</name>
  <files>bot/handlers.py</files>
  <action>
1. Store transcript language info in pending_video_meta (alongside other video metadata):
   - "source_language": transcription_result.language
   - "source_transcript": transcription_result.text
   - "source_transcript_en": transcription_result.english_text

2. Update select_place_callback() to pass language metadata when saving:
   - source_language=video_meta.get("source_language")
   - source_transcript=video_meta.get("source_transcript")
   - source_transcript_en=video_meta.get("source_transcript_en")

3. Optionally show detected language in confirmation if non-English:
   - "🌏 Detected: {language_name}" (only if language != "en")
   - Keep it subtle - one line at most

Keep messages concise. Language detection is informational, not critical to show.
  </action>
  <verify>Manual test: Multi-place selection with non-English video saves language metadata</verify>
  <done>select_place_callback saves language metadata, optional language display</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/ -v` passes ALL tests
- [ ] Non-English video uses English translation for place search
- [ ] Language metadata stored in database (source_language, source_transcript, source_transcript_en)
- [ ] Both single-place and multi-place paths save language info
- [ ] Messages remain friendly and not cluttered
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Place search uses English text for better results
- Language info preserved in database
- Phase 5 complete: Language Support shipped
</success_criteria>

<output>
After completion, create `.planning/phases/05-language-support/05-02-SUMMARY.md`

Final summary should note:
- English translation used for Google Places search
- Language metadata stored for reference
- Phase 5 complete, ready for Phase 6 (Interactive Viewer)
</output>
