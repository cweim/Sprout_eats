---
phase: 12-critical-ux-fixes
plan: 03
type: execute
---

<objective>
Improve list card tap behavior and review editing UX.

Purpose: List card tap behavior is confusing (switches to map). Review save button text doesn't distinguish new vs edit.
Output: Better list card feedback, clearer review editing UX.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/UX_AUDIT.md
@webapp/app.js
@webapp/styles.css

**From UX Audit:**
> 🔴 **CRITICAL: Tapping card opens map popup, not place details**
> - Unexpected behavior - tapping should show details
> - Map popup requires switching views
> - **Fix:** Either:
>   - Option A: Open place detail sheet (new)
>   - Option B: Keep map popup but add visual feedback it's switching views

> 🟡 **MEDIUM: Can't edit review from Reviews tab**
> Already implemented! openReviewSheetFromHistory() opens editable review sheet.
> But need clearer UX:
> - Save button should say "Update Review" when editing existing review
> - Delete button visibility handled correctly

**Current behavior:**
- List card click → switchView('map') + center on place + open popup
- Review sheet always shows "Save Review" button
- Delete button shown only when currentReview.id exists (correct)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add visual transition feedback for list card tap</name>
  <files>webapp/app.js, webapp/styles.css</files>
  <action>
When user taps a place card in list view, add clear visual feedback before switching to map:

1. In app.js, modify the card click handler in createPlaceCard():
   - Add brief visual feedback (card highlight/pulse animation)
   - Show subtle toast: "Opening on map..."
   - Then switch to map view

2. In styles.css, add:
   - .place-card.tapped animation (brief scale + highlight)
   - Animation should be fast (~200ms) so transition feels snappy

This provides clear feedback that tapping does something intentional.

Alternative approach (simpler): Just show the toast "📍 Showing on map..." and proceed.
  </action>
  <verify>Tap place card, see toast feedback, then map opens to that place</verify>
  <done>List card tap shows clear feedback before switching to map view</done>
</task>

<task type="auto">
  <name>Task 2: Update review button text based on edit mode</name>
  <files>webapp/app.js</files>
  <action>
In openReviewSheet() and related functions:

1. Track whether we're editing existing vs new review (use currentReview.id presence)
2. Update save button text based on mode:
   - New review: "Save Review"
   - Editing existing: "Update Review"

In openReviewSheet():
```javascript
const saveBtn = document.getElementById('save-review-btn');
if (currentReview && currentReview.id) {
    saveBtn.textContent = 'Update Review';
} else {
    saveBtn.textContent = 'Save Review';
}
```

Also update after loading existing review data (in the response.ok block).
  </action>
  <verify>Open new review - shows "Save Review". Open existing review - shows "Update Review"</verify>
  <done>Save button text reflects new vs edit mode</done>
</task>

<task type="auto">
  <name>Task 3: Add review indicator badge to list card names</name>
  <files>webapp/app.js</files>
  <action>
Verify review badge shows on place cards in list view (already implemented per code).

Current code in createPlaceCard():
```javascript
const review = getPlaceReview(place.id);
const reviewBadge = review
    ? `<span class="place-review-badge">✍️ ${'⭐'.repeat(review.overall_rating)}</span>`
    : '';
```

Verify this works and the badge is visible. If not styled properly, add CSS.

Also ensure review badges update when reviews are saved/deleted (check if renderPlacesList is called after review operations).
  </action>
  <verify>Save a review, list view shows ✍️⭐⭐⭐ badge on that place card</verify>
  <done>Review badges appear on place cards and update in real-time</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] List card tap shows feedback toast before map switch
- [ ] Save button shows "Update Review" when editing existing review
- [ ] Review badges visible on place cards with reviews
- [ ] Badges update when reviews saved/deleted
- [ ] No console errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- User understands list card tap navigates to map
- Review editing mode is clear from button text
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-ux-fixes/12-03-SUMMARY.md`
</output>
