---
phase: 12-critical-ux-fixes
plan: 02
type: execute
---

<objective>
Add photo upload progress indicator and ARIA accessibility labels to all interactive elements.

Purpose: Users see no feedback during photo upload (can take 5s). Screen readers cannot identify icon-only buttons.
Output: Visual upload progress in photo grid, aria-labels on all icon buttons.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/UX_AUDIT.md
@webapp/index.html
@webapp/app.js
@webapp/styles.css

**From UX Audit:**
> 🔴 **CRITICAL: Photo upload has no progress indicator**
> - After selecting: "Uploading photo..." toast appears
> - If upload takes 5s, user sees nothing
> - **Fix:** Show upload progress bar in photo grid

> 🔴 **CRITICAL: No ARIA labels**
> - Icon buttons have no labels
> - Screen reader says "Button" for ✍️
> - **Fix:** Add aria-label="Write review" to all icon buttons

**Buttons needing aria-label (from index.html):**
- Map controls: btn-my-location (📍), btn-fit-all (🗺️)
- Modal close buttons: ×
- Filter button: ⚙️
- Search clear: ×
- FAB discover: 🔍
- Sheet close buttons
- Drawer close buttons
- Action buttons in popups (📍, ▶️, 🗑️, ✍️)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ARIA labels to all interactive elements in HTML</name>
  <files>webapp/index.html</files>
  <action>
Add aria-label attributes to all icon-only buttons:

1. Map controls:
   - btn-my-location: aria-label="Center on my location"
   - btn-fit-all: aria-label="Show all places"

2. Modal/sheet close buttons:
   - All × buttons: aria-label="Close"
   - search-modal-close, modal-close, filter-drawer-close, review-sheet-close

3. Filter/search:
   - filter-btn: aria-label="Open filters"
   - search-clear: aria-label="Clear search"
   - fab-discover: aria-label="Discover new places"

4. Place menu items already have text, but add aria-label to menu-delete: aria-label="Delete this place"

Also add role="dialog" to modals/sheets for proper screen reader context.
  </action>
  <verify>Grep for aria-label in index.html, should find 10+ occurrences</verify>
  <done>All icon buttons have descriptive aria-labels, modals have role="dialog"</done>
</task>

<task type="auto">
  <name>Task 2: Add ARIA labels to dynamically created elements in JS</name>
  <files>webapp/app.js</files>
  <action>
Add aria-label to buttons created in JavaScript:

1. In createPopupContent():
   - Review button: aria-label="Write review for {place.name}"
   - Google Maps link: aria-label="Open {place.name} in Google Maps"
   - Original reel link: aria-label="View original reel"
   - Delete button: aria-label="Delete {place.name}"

2. In createPlaceCard():
   - Any icon-only buttons get aria-labels

3. In photo grid (renderDishPhotos, renderOverallPhotos):
   - Add button: aria-label="Add photo"
   - Remove button: aria-label="Remove photo"

4. In star rating creation:
   - Star buttons: aria-label="{n} star" or aria-label="{n} stars"
  </action>
  <verify>Search app.js for aria-label, should find in popup, card, and photo functions</verify>
  <done>All dynamically created icon buttons have aria-labels</done>
</task>

<task type="auto">
  <name>Task 3: Add photo upload progress indicator</name>
  <files>webapp/app.js, webapp/styles.css</files>
  <action>
In app.js, modify photo upload flow to show progress:

1. When photo selected, show placeholder in grid immediately with loading spinner
2. During upload, show progress bar overlay on the placeholder
3. On success, replace placeholder with actual photo
4. On error, show error state with retry option

In uploadPhoto() or equivalent:
- Create placeholder element: `<div class="photo-uploading"><div class="upload-spinner"></div><div class="upload-progress-bar"><div class="progress-fill"></div></div></div>`
- Update progress-fill width during XHR upload (use xhr.upload.onprogress)
- On complete, replace with actual photo

In styles.css, add:
- .photo-uploading styles (centered spinner, subtle bg)
- .upload-progress-bar styles (thin bar at bottom of photo slot)
- .progress-fill animation
  </action>
  <verify>Upload a photo and observe progress indicator appears during upload</verify>
  <done>Photo upload shows visual progress bar, placeholder during upload, smooth transition to final photo</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All static icon buttons in HTML have aria-label
- [ ] All dynamic icon buttons in JS have aria-label
- [ ] Photo upload shows progress indicator
- [ ] Screen reader can identify all interactive elements
- [ ] No TypeScript/JS errors in console
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Accessibility: every interactive element identifiable by screen reader
- UX: photo upload has clear visual feedback
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-ux-fixes/12-02-SUMMARY.md`
</output>
