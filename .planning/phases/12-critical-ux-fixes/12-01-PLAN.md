---
phase: 12-critical-ux-fixes
plan: 01
type: execute
---

<objective>
Add granular progress feedback during bot video processing.

Purpose: Users currently see generic "Checking the post..." message for 30-60s operations. Need step-by-step status updates so users know the bot is working.
Output: Bot sends distinct status messages for each processing stage with friendly emojis.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/UX_AUDIT.md
@bot/handlers.py

**From UX Audit (Flow A: Discovery):**
> 🔴 **CRITICAL: No progress feedback during processing**
> - Video download/transcription can take 30-60s
> - User sees nothing after sending URL
> - **Fix:** Add "Downloading video... 📥" → "Transcribing audio... 🎤" → "Finding places... 🔍" status messages

**Current state:** `handle_url()` in handlers.py already has some status messages but they're inconsistent:
- "Ooh, fresh content! Let me dig in... 🔍" (initial)
- "Checking the post... 📥" (download)
- "Reading the caption... 📝" (caption)
- "Listening carefully... 🎧" (transcription)
- "Hunting for places... 🔍" (search)

Need to ensure ALL stages show distinct, friendly progress.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve progress message consistency in handle_url</name>
  <files>bot/handlers.py</files>
  <action>
Update `handle_url()` to show clearer, more distinct progress messages at each stage:

1. Initial: "Ooh, fresh content! Let me check this out... 🔍"
2. Download: "Downloading video... 📥" (change from "Checking the post")
3. Caption check: "Reading the caption... 📝" (keep)
4. OCR: "Scanning images for text... 🖼️" (change from "Reading text in the photos")
5. Transcription: "Transcribing audio... 🎤" (change from "Listening carefully")
6. Place search: "Finding places... 🔎" (change from "Hunting for places")

Each message should be visually distinct with different emojis. Keep the cute, friendly tone.
  </action>
  <verify>Read handlers.py and confirm all status_msg.edit_text calls use the new messages</verify>
  <done>All processing stages show distinct progress messages with appropriate emojis</done>
</task>

<task type="auto">
  <name>Task 2: Add progress messages to multi-place selection flow</name>
  <files>bot/handlers.py</files>
  <action>
In the multi-place selection flow (save_selected_callback), add progress feedback:

1. After "Saving..." answer, edit message to show "Saving your places... 💾"
2. After successful save, show personalized message:
   - Single place: "✨ Saved **{name}** to your map!"
   - Multiple: "✨ Saved {count} places to your map!"

Use bold markdown for place names. Keep cute tone.
  </action>
  <verify>Test save flow shows progress and personalized success messages</verify>
  <done>Save callback shows progress and personalized success with place names</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All handle_url status messages are distinct and emoji-coded
- [ ] Messages follow consistent pattern: action + emoji
- [ ] Save flow shows progress and success with place names
- [ ] No duplicate or confusing messages
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Bot provides clear, step-by-step feedback during processing
- Users know exactly what stage the bot is at
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-ux-fixes/12-01-SUMMARY.md`
</output>
